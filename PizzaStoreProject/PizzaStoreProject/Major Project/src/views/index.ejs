<!DOCTYPE html>
<html>
<head>
    <title>Pizza Store</title>
</head>
<body>
    <h1>Pizza Store Items</h1>
    <ul>
        <% items.forEach(function(item) { %>
            <li><%= item.name %> - <%= item.price %></li>
        <% }); %>
    </ul>
</body>
</html>
